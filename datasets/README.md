# Dataset Directory

This directory is intentionally kept lightweight in the public repository.

For local runs, either:

- create a symlink named `datasets` that points to your shared dataset storage, or
- place the required raw/processed datasets here following the instructions in the top-level `README.md`.
